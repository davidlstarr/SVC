import { mapviewConfig } from './mapview.config';
import { layersConfig } from './layers.config';
import { sidePanelConfig } from './sidePanel.config';
import { chartDataUrl } from './chart.config';
import { serviceUrls } from './serviceUrls';
import { defaultLayersConfig } from './defaultLayers.config';
import { wetdryTooltipDescription } from './wetdryTooltipDescription';
import { AOILayersConfig } from './AOILayers.config';

const config = {
  mapviewConfig,
  layersConfig,
  sidePanelConfig,
  chartDataUrl,
  serviceUrls,
  defaultLayersConfig,
  AOILayersConfig,
  wetdryTooltipDescription,
};

export default config;
